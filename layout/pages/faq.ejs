<section class="section-block prose-block">
  <% theme.faq.forEach((item) => { %>
    <article class="policy-block">
      <h2><%= item.q %></h2>
      <p><%= item.a %></p>
    </article>
  <% }) %>
</section>
